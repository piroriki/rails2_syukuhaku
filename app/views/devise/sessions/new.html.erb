<%= render partial: "devise/shared/partial-header" %>

<div class="user-field">
<h4>ログイン</h4>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <div id="required">
    <td><%= f.email_field :email, placeholder: "メールアドレス" %></td><br />
    </div>

    <div id="required">
    <td><%= f.password_field :password, placeholder:"パスワード" %></td><br />
    </div>

  <% if devise_mapping.rememberable? %>
      <td><%= f.check_box :remember_me %></td>
      <th><%= f.label :remember_me,"パスワードを記憶する" %></th><br />
  <% end %>

    <%= f.submit "ログイン" %>
<% end %>
</div>

<%= render partial: "devise/shared/partial-footer" %>